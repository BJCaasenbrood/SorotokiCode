function str = whoClasses(ClassName)
evalin('base', 'clearvars ans;');
s = evalin('base', 'whos');
data_collector = zeros(size(s));
for i = 1:length(s)
    k = s(i).class;
    data_collector(i) = strcmp(k,ClassName); 
    
    if data_collector(i) == 1; 
        (s(i).name);
    end
end

end

