clr;
%% model
obj = Gmodel('Pneunet.stl');

%% deform
Blender(obj,'Translate',{'y',-0.4});
Blender(obj,'Curve',{'PCC+',120,0,0.8});
Blender(obj,'Rotate',{'y',-90});

%% show
obj.bake().render(); 
obj.reset().bake().render(); 
axis tight;
